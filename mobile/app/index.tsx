import { View } from 'react-native';

// Placeholder shell. Deliberately text-free: user-visible strings must go through i18n (step 4).
export default function Index() {
  return <View testID="home-screen" style={{ flex: 1 }} />;
}
